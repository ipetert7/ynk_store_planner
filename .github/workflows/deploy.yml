name: Deploy YNK Modelo (Prod)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy on server
        run: |
          cd /opt/ynk-modelo
          if [ -x "./ynk_main/deploy.sh" ]; then
            cd ./ynk_main
            ./deploy.sh
          elif [ -x "./deploy.sh" ]; then
            ./deploy.sh
          else
            echo "No deploy script found at ./deploy.sh or ./ynk_main/deploy.sh"
            exit 127
          fi
